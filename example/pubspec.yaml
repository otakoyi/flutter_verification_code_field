name: flutter_verification_code_field_example
description: Demonstrates how to use the flutter_verification_code_field.
publish_to: 'none'  

environment:
  sdk: '^3.6.1'

dependencies:
  flutter:
    sdk: flutter
  flutter_verification_code_field:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
